import React from "react";
import AuthFormsContainer from "../Auth/AuthFormsContainer";

const LoginPage = () => {
  return <AuthFormsContainer defaultIsLogin={true} />;
};

export default LoginPage;
