import React from "react";
import AuthFormsContainer from "../Auth/AuthFormsContainer";

const RegisterPage = () => {
  return <AuthFormsContainer defaultIsLogin={false} />;
};

export default RegisterPage;